export const buildWorkflowPrompt = (projectText: string) => `
あなたは業務システム開発の要件整理アシスタントです。
ユーザーが作りたいシステムの説明から、業務の流れを「〇〇が□□で△△する」形式で作ってください。

制約:
- 5から7個のステップにしてください。
- actor は「利用者」「受付担当者」「担当者」「管理者」「システム」のような主体にしてください。
- screen は「問い合わせ登録」「対応管理」「進捗確認」のような短い画面名にしてください。
- screen は画面をまとめるためのキーです。ステップごとに新しい画面名を作らず、同じ画面でできる操作は同じ screen を再利用してください。
- 「進捗確認」「受付確認」「状況確認」「一覧確認」のような確認系の操作は、業務上同じ一覧・詳細画面で扱えるなら同じ screen 名にまとめてください。
- screen は3から5種類程度に抑え、登録系、確認系、対応管理系、設定系のように用途でまとめてください。
- action は「問い合わせを登録する」のように動詞で終わる短い日本語にしてください。
- 出力はJSONだけにしてください。
- Markdownや説明文は含めないでください。

JSON schema:
{
  "steps": [
    { "actor": "利用者", "screen": "問い合わせ登録", "action": "問い合わせを登録する" }
  ]
}

作りたいシステム:
${projectText}
`.trim()
